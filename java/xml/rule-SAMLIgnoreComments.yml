# yamllint disable
# License: MIT (c) GitLab Inc.
# yamllint enable
---
rules:
- id: "java/xml/rule-SAMLIgnoreComments"
  pattern: "(BasicParserPool $POOL).setIgnoreComments(false);"
  languages:
  - "java"
  message: |
    Ignoring XML comments in SAML may lead to authentication bypass
  metadata:
    # yamllint disable
    source-rule-url: "https://find-sec-bugs.github.io/bugs.htm#SAML_IGNORE_COMMENTS"
    # yamllint enable
    category: "security"
    confidence: "HIGH"
  severity: "ERROR"
