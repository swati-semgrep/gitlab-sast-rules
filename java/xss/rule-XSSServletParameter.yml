# yamllint disable
# License: MIT (c) GitLab Inc.
# yamllint enable
---
rules:
- id: "java/xss/rule-XSSServletParameter"
  patterns:
  - pattern-inside: |-
      $TYPE $FUNC(..., HttpServletRequest $REQ, ...) { ... }
  - pattern-inside: |-
      $REQ.getParameter(...);
  message: |
    Untrusted servlet parameter
  languages:
  - "java"
  severity: "WARNING"
  metadata:
    cwe: "CWE-20: Improper Input Validation"
    owasp: "A7: Cross-Site Scripting (XSS)"
    # yamllint disable
    source-rule-url: "https://find-sec-bugs.github.io/bugs.htm#SERVLET_PARAMETER"
    # yamllint enable
    category: "security"
    confidence: "LOW"
    severity: "LOW"
    technology:
    - "java"
